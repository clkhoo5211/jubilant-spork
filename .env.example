# NOFX Docker Environment Configuration
# These ports can be customized to avoid conflicts with other services

# Backend API port (external host port)
# The container always listens on 8080 internally, but you can map it to any host port
NOFX_BACKEND_PORT=8888

# Frontend port (external host port)
# The container always listens on 80 internally, but you can map it to any host port
NOFX_FRONTEND_PORT=8000

# Timezone
NOFX_TIMEZONE=Asia/Shanghai
